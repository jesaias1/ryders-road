# Module folders

Folders mirror product capabilities so later phases can grow without moving
unrelated files. A folder with only a `.gitkeep` has no Phase 0 runtime and is
reserved—not an invitation to add placeholders. Runtime code belongs in the
assembly that owns its behavior; reusable contracts belong in Core.
