## 0.9.7 autonomous production continuation

The user's 2026-09-08 mission authorizes independent production beyond historical
STOP gates; candidate movement promotion still requires physical approval.
ADR 0019 and Docs/PRODUCTION_097.md record the isolated Flow Lab landing-view
comparison, ordinary precision room, session-best isolation, contact foley and
Ancient Abyss startup preservation. Campaign motor/input/profile/save/content
bytes are preserved. Current verification and artifact: Docs/AI_HANDOFF.md.
No physical approval, final audio mix or production freeze is claimed.
## Movement Mastery 0.9.6 verification

223/223 EditMode and 48/48 PlayMode pass; source validation and ARM64 APK build
pass. New runtime-controller tests cover normalized Flow input, five-hop chains,
surf departure/contact, walls/reset, room completion and real frontend/input flow.
30/60/120 Hz simulated motor steps do not certify phone feel. An initial existing
Editor video timeout passed on unchanged full rerun. S23 install/startup only was
verified; human movement/performance acceptance remains open. Full evidence and
physical checklist: Docs/MOVEMENT_MASTERY_SLICE.md.

<!-- Current follow-up: 0.9.5-startup-course-clearance -->

0.9.5 addresses physical startup/UI and course intersections. See
Docs/S23_095_FIRST_THREE_FEEDBACK.md and ADR 0017. Module 002 v3 / 003 v7 retain
stable IDs and the unchanged motor; whole-path clearance supplements top-ray and
endpoint traversal tests. Native startup uses the approved loading poster.

# RYDER'S ROAD Testing

## 0.9.4 Mountain World authority

Verify fresh Campaign locks, Bronze Next, Continue, replay and legacy valid progress with isolated save fixtures. Exercise every ordinary Module 002 jump, Moving docks/carry, near collision, Restores, shards and Retry. Inspect five real-FOV mountain views. Full regression/build evidence belongs in Docs/S23_094_MOUNTAIN_WORLD.md; Editor checks do not certify phone performance.

## 0.9.3 Sky City verification

Capture and critically inspect opening, bridge, garden, gateway and Patch approach at real 94-degree gameplay FOV. Validate authored collision, route supports, ordinary jump reach, stable progression IDs, bounded geometry and preservation through Editor setup. Full source/EditMode/PlayMode/build evidence is recorded in Docs/S23_093_SKY_CITY.md. Editor evidence cannot certify physical S23 feel or 60 FPS.

## 0.9.2 real navigation gate

The 0.9.1 runner-owned loading test missed destruction of the real UI caller.
HotfixNavigationTests invokes actual Campaign/Spiral/card/Retry/Next/recovery buttons
and PatchBlock completion. It checks destination identity, player control, overlay
alpha and raycast release, unavailable video, throwing presentation subscribers,
missing scenes and readiness timeout. The viewport gate captures menu, Campaign,
loading and Module 003 at S23 aspect and checks COVER bounds/full camera rect.
Editor frame advancement is required but cannot certify Android decoding.
Final evidence and APK are recorded in Docs/S23_092_HOTFIX.md.

## 0.9.1 collision truth, flow and product QA

211 EditMode / 11 PlayMode cases; final evidence in
`Logs/phase091-editmode-final-results.xml` and
`Logs/phase091-playmode-final-results.xml`. Source validation:
`Logs/phase091-source-validation.log`. Build evidence is recorded after packaging
in `Docs/AI_HANDOFF.md`.

Coverage adds exact authored near-mesh collision before/after static batching,
supporting-ray samples across all 37 near structural meshes, route/mechanic box
supports, long-platform visible bounds, all three Restore shrine footprints,
actual optional pickup triggers and duplicate protection, bounded haptic cue
mapping, loading/menu/Campaign/HUD/results rendering. Existing Boost tests still
launch the real motor through the actual trigger at slow/nominal/full speeds.
Classic/manual camera, hidden arms, prior modules, save/progression, Crumble and
Restore regression tests remain. Five views and frontend captures are in
`Logs/Phase091VisualQA`; captures do not establish phone acceptance.

Physical S23 acceptance remains required: complete Bronze and mastery routes,
attractive ruin/grass landings and recovery, Moving boarding/exit, Boost aim and
landing feel, one-second Crumble escape, shard collection/Restore/Retry, native
haptic intensity and setting, loading first frame/video aspect/resume, both
landscape safe areas, fresh/existing saves, Bronze Next and sustained 60 FPS.
No physical device testing was performed by the agent in this continuation.


## 0.9.0 Astra milestone verification

- EditMode: 208/208, `Logs/phase090-editmode-final-results.xml`.
- PlayMode: 9/9, `Logs/phase090-playmode-final-results.xml`.
- Source validation: `Logs/phase090-source-validation.log`.
- `Module003_BoostTriggerLandsRealMotorOnBronzeDestination` uses the actual
  trigger and CharacterController at 3, 5 and full base-run speed; it requires
  the intended landing collider and more than 0.5 m center-to-edge margin.
- New tests protect both landscape directions with no portrait, exact Moving
  endpoint dwell for both easings, zero-pause/repeated-point paths, Campaign
  successor isolation from Spiral/labs, PB-focused results and silent audio
  that still emits presentation intent. Foundation checks catch imported-axis
  scaling mistakes and require decorative foundation geometry well below its place origin.
- Final five-view renders: `Logs/Phase090VisualQA`. Art gate remains open;
  automated rendering and landing tests do not establish fun or phone comfort.
- Android log: `Logs/phase090-android-build-final.log`; final APK evidence is in
  `Docs/AI_HANDOFF.md`. No physical testing was performed in this run.


## Phase 0.7.9 Module 003 First Finished Level Evidence

- Source validation passed: `Logs/phase079-source-validation.log`.
- Foundation validation passed: `Logs/phase079-foundation-validation.log`.
- EditMode passed `194/194`: `Logs/phase079-editmode-results.xml`.
- PlayMode passed `7/7`: `Logs/phase079-playmode-results.xml`.
- Focused coverage verifies the first ten Module 003 visible supports use
  non-trigger colliders, the old water trigger and grounding disc are absent,
  ordinary blocks have no active VFX, the full normal route remains within the
  movement envelope, special supports do not overlap normal blocks, Restores
  and Crumble timing remain valid, arms stay hidden, and camera/movement smoke
  checks still pass across Module 001, Module 002, Module 003, and Spiral.
- Android ARM64 IL2CPP build succeeded with zero errors and one legacy-icon
  deprecation warning: `Logs/phase079-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.7.9-module003-first-finished-level-dev.apk`,
  172,134,924 bytes, SHA-256
  `1694EE3331CBE117DB07CB4BA3D802B3BB9BE2CBFF26D6C1526AB53DF64DE2F2`.
- No ADB/device query, install, launch, screenshot, physical gameplay, FPS, or
  thermal check was performed. Physical Samsung S23 acceptance remains pending.

## Phase 0.7.7 Camera Recovery And Platform Visual Truth Evidence

- Source validator passed: `Logs/phase077-source-validator.log`.
- Foundation validator passed: `Logs/phase077-foundation-validator.log`.
- EditMode passed `193/193`: `Logs/phase077-editmode-results.xml`.
- PlayMode passed `6/6`: `Logs/phase077-playmode-results.xml`.
- Focused coverage verifies `+9` default pitch, `-75/+75` clamps, manual touch
  pitch, flow-mode vertical-only right drag, unchanged `94` FOV, Tap Jump
  classification, immediate Reset pitch application, and pitch-preserving
  fall Restore.
- The ModuleRunner smoke test launches Module 001, Module 002, Module 003, and
  Spiral, changes pitch in each, confirms arms hidden and grounding active,
  rejects every obsolete cap-shell object, and compares authored block scale
  with the unchanged root `BoxCollider` presentation.
- Android ARM64 IL2CPP development build succeeded with one legacy-icon
  deprecation warning and zero errors: `Logs/phase077-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.7.7-camera-recovery-platform-visual-truth-dev.apk`,
  172,100,080 bytes, SHA-256
  `80356AF9566E932BF7CC3E885F42EEFE9E00E643252185AAEAD79D34FF35E43A`.
- No ADB/device query, install, launch, screenshot, physical gameplay, FPS,
  thermal, or artifact check was performed. Physical S23 acceptance remains
  pending.

## Phase 0.7.6 Global Readability And Module 003 Art Pass Evidence

- Source validator passed: `Logs/phase076-source-validator.log`.
- Foundation validator passed: `Logs/phase076-foundation-validator.log`.
- EditMode passed `190/190`: `Logs/phase076-editmode-results.xml`.
- PlayMode passed `6/6`: `Logs/phase076-playmode-results.xml`.
- The live ModuleRunner smoke check loads Module 001, Module 002, Module 003,
  and Spiral and asserts global `-9` degree pitch, `94` FOV, hidden arms,
  grounding profile/cue, and collider-truth platform shell. Spiral Restore also
  verifies preserved death yaw and restored fixed pitch.
- Android ARM64 IL2CPP development build succeeded with one legacy-icon
  deprecation warning and zero errors: `Logs/phase076-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.7.6-global-readability-module003-art-pass-dev.apk`,
  172,131,236 bytes, SHA-256
  `62829520B4339ED7A00537E1EA21A579B2DDC803DABBE99D1209635A5DD382F8`.
- By instruction, 0.7.6 did not perform ADB discovery, pairing, install,
  launch, screenshot, physical gameplay, FPS, thermal, or artifact checks.
  Physical S23 acceptance remains pending.

## Phase 0.7.5 Module 003 Gold-Standard Vertical Slice Evidence

- Source validator: passed via `Tools/Validate-Foundation.ps1`; final output
  saved in `Logs/phase075-gold-source-validator-final.log`.
- Foundation validator: passed in
  `Logs/phase075-gold-foundation-validator.log`.
- EditMode: `188/188` passed in
  `Logs/phase075-gold-editmode-results.xml`.
- PlayMode: `6/6` passed in
  `Logs/phase075-gold-playmode-results.xml`.
- Added focused automated coverage for hidden-but-preserved first-person arms,
  hidden default developer telemetry, Ancient Abyss depth-band placement,
  moving mist layers, far hero landmark placement, and near decoration scale.
- Android IL2CPP ARM64 development build succeeded with one warning and zero
  errors in `Logs/phase075-gold-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.7.5-module003-gold-standard-vertical-slice-dev.apk`,
  172,055,830 bytes, SHA-256
  `0B95C62BAD98183D5E8AC20019466142189F57C724FE6B6FAEE2EAF05F8D4375`.
- Not physically validated: Samsung S23 was not connected by instruction. No
  ADB device scan, install, launch, screenshot, physical gameplay, FPS,
  fullscreen, thermal, or touch-comfort check was performed. USER PHYSICAL
  ACCEPTANCE PENDING.

## Phase 0.7.3 Campaign Recovery, Biomes, And World Assets Evidence

- Source validator: passed via `Tools/Validate-Foundation.ps1`; output saved
  in `Logs/phase073-campaign-biomes-source-validator-final.txt`.
- Foundation validator: passed in `Logs/phase073-foundation-validator.log`.
- EditMode: `186/186` passed in
  `Logs/phase073-campaign-biomes-editmode-results.xml`.
- PlayMode: `6/6` passed in
  `Logs/phase073-campaign-biomes-playmode-results.xml`.
- Added focused automated coverage for Campaign biome assignments, collider-free
  prepared world prefabs, full Bronze unlock chain from Module 001 to Module
  003, and Spiral exclusion from the normal Campaign chain.
- Imported 29 curated Kenney CC0 FBX models, prepared 29 collider-free
  background prefabs, and created four `EnvironmentBiomeProfile` assets.
  License records are in `Docs/THIRD_PARTY_ASSETS.md`.
- Android IL2CPP ARM64 development build succeeded with one warning and zero
  errors in `Logs/phase073-campaign-biomes-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.7.3-campaign-recovery-biomes-and-world-assets-dev.apk`,
  171,974,630 bytes, SHA-256
  `4A87EF727E18DCC25BD95CD66E40369388FB849C0CB47B009A946F001726C1F2`.
- Not physically validated: Samsung S23 was not connected by instruction. No
  ADB device scan, install, launch, screenshot, physical gameplay, FPS,
  fullscreen, thermal, or touch-comfort check was performed. USER PHYSICAL
  ACCEPTANCE PENDING.

## Phase 0.7.3 Surgical Arm Presentation Recovery Evidence

- Source validator: passed via `Tools/Validate-Foundation.ps1`; final output
  saved in `Logs/phase073-source-validator.txt`.
- Foundation validator: passed in `Logs/phase073-foundation-validator.log`.
- EditMode: `185/185` passed in `Logs/phase073-editmode-results.xml`.
- Import audit:
  `Logs/phase073-surgical-arm-presentation-recovery-audit.txt` records the
  unchanged V2 mesh counts, 24-joint source-right/baked-left hierarchy,
  positive scales, recovered higher base pose, 58-degree elbow bend,
  68-degree mirrored wrist pronation, restrained digit curl, and visual-only
  low-amplitude locomotion additive state.
- HandPoseLab numeric diagnostics:
  `Logs/HandPoseLab/final-axis-acceptance.txt`. The regenerated
  `Logs/HandPoseLab/final.png` remained the same small batch-render artifact
  in this environment, so it is not used as visual acceptance evidence.
- Not run by instruction/scope: no ADB, no phone automation, no Android
  install/launch/screenshot, and no Android build. S23 FPS/fullscreen/framing
  and final human arm acceptance remain pending.

## Phase 0.7.2 Evidence

- Source validator: passed via `Tools/Validate-Foundation.ps1`; final output
  saved in `Logs/phase072-source-validator-final.txt`.
- Foundation validator: passed in `Logs/phase072-foundation-validator.log`.
- EditMode: `185/185` passed in `Logs/phase072-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase072-playmode-results.xml`.
- Import audit:
  `Logs/phase072-first-person-arm-reference-lock-audit.txt` records the
  unchanged V2 mesh counts, 24-joint source-right/baked-left hierarchy,
  positive scales, 0.7.2 lower-corner reference-locked base pose, 58-degree
  elbow bend, 62-degree mirrored wrist pronation, restrained digit curl, and
  visual-only low-amplitude locomotion additive state.
- HandPoseLab numeric diagnostics:
  `Logs/HandPoseLab/final-axis-acceptance.txt`. The regenerated
  `Logs/HandPoseLab/final.png` remained blank gray in this batch environment,
  so it is not used as visual acceptance evidence.
- Android IL2CPP ARM64 development build succeeded with zero errors and one
  warning in `Logs/phase072-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.7.2-first-person-arm-reference-lock-dev.apk`,
  171,679,642 bytes, SHA-256
  `F2A5E124CFFAF05380F51DC6E91F50A70E99293299E634C22681E433564AFA62`.
- Not physically validated: by instruction, the phone was not connected and no
  ADB/device automation, install, launch, screenshot, or physical gameplay was
  attempted. S23 FPS/fullscreen/framing and final human arm acceptance remain
  pending.

## Phase 0.7.1 Evidence

- Source validator: passed via `Tools/Validate-Foundation.ps1`; output saved
  in `Logs/phase071-source-validator.txt`.
- Foundation validator: passed in `Logs/phase071-foundation-validator.log`.
- EditMode: `185/185` passed in `Logs/phase071-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase071-playmode-results.xml`.
- Import audit:
  `Logs/phase071-ryder-arm-v2-framing-and-animation-lock-audit.txt` records
  the unchanged V2 mesh counts, 24-joint source-right/baked-left hierarchy,
  positive scales, 0.7.1 outward/lower/back base pose, 58-degree elbow bend,
  softened 68-degree mirrored wrist pronation, restrained digit curl, and
  visual-only outward-safe locomotion additive state.
- HandPoseLab numeric diagnostics:
  `Logs/HandPoseLab/final-axis-acceptance.txt`. The regenerated
  `Logs/HandPoseLab/final.png` remained blank gray in this batch environment,
  so it is not used as visual acceptance evidence.
- Android IL2CPP ARM64 development build succeeded with zero errors and one
  warning in `Logs/phase071-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.7.1-first-person-arm-framing-and-animation-lock-dev.apk`,
  171,681,386 bytes, SHA-256
  `110C5E1614DAE8599DAC9EB727C641D567E45D494C4052076A19B7007E5E527B`.
- Not physically validated: the user's Samsung S23 was not connected for this
  phase by instruction. No install, launch, screenshots, ADB reconnect loops,
  or physical gameplay were attempted. S23 FPS/fullscreen/framing and final
  human arm acceptance remain pending.

## Phase 0.7.0 Evidence

- Source validator: passed via `Tools/Validate-Foundation.ps1`; final output
  saved in `Logs/phase070-source-validator-final.txt`.
- Foundation validator: passed in `Logs/phase070-foundation-validator.log`.
- EditMode: `184/184` passed in
  `Logs/phase070-editmode-results-final.xml`.
- PlayMode: `6/6` passed in `Logs/phase070-playmode-results-final.xml`.
- Import audit:
  `Logs/phase070-ryder-arm-v2-animation-lock-audit.txt` records the unchanged
  V2 mesh counts, 24-joint source-right/baked-left hierarchy, positive scales,
  current base pose, 58-degree elbow bend, mirrored wrist pronation, restrained
  digit curl, and visual-only locomotion additive state.
- HandPoseLab numeric proof:
  `Logs/HandPoseLab/final-axis-acceptance.txt`. In this batch environment the
  regenerated `Logs/HandPoseLab/final.png` was blank gray, so it is not used as
  visual acceptance evidence for this phase.
- Android IL2CPP ARM64 development build succeeded with zero errors and one
  warning in `Logs/phase070-android-build-final.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.7.0-first-person-arm-camera-and-animation-lock-dev.apk`,
  171,677,950 bytes, SHA-256
  `0BC88239B6CF4B1E0FB6FC1E42CB60D791467B7C468F655482FE58182484BFF7`.
- Not physically validated: ADB was available, but no Android device was
  connected during the smoke window. S23 install/launch, landscape rotation,
  Spiral gameplay framing, sustained FPS and thermals, full route traversal,
  pause/resume, and final human arm-pose/material acceptance remain pending.

## Phase 0.6.9 Evidence

- Source validator: passed via `Tools/Validate-Foundation.ps1`.
- Foundation validator: passed in `Logs/phase069-foundation-validator.log`.
- EditMode: `183/183` passed in `Logs/phase069-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase069-playmode-results.xml`.
- Import audit: `Logs/phase069-ryder-arm-v2-import-audit.txt` records the
  byte-identical source copy, 11,953 vertices, 15,541 triangles, 24 joints,
  source-right/baked-left handedness, positive scales, material maps, and
  static elbow/wrist/digit pose.
- Production render: `Logs/HandPoseLab/final.png`.
- Android IL2CPP ARM64 development build succeeded with zero errors and one
  warning in `Logs/phase069-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.9-ryder-arm-v2-full-replacement-dev.apk`,
  171,648,490 bytes, SHA-256
  `49F7D83940545347E3C08CE519E0ED532C0AE76EE7C85C6BCF396999BAC7C0D0`.
- Bounded S23 smoke: installed on Samsung Galaxy S23 `SM-S911B`
  (`RFCW100J1HW`), launched the Unity activity, rendered fullscreen landscape
  at 2340x1080, and loaded Spiral with both V2 arms visible. Diagnostics showed
  `FPS 60`, `Ground True`, and `PARKOUR_RESPONSIVE_PHYSICAL_V2`.
- Evidence: `Logs/phase069-s23-menu.png`,
  `Logs/phase069-s23-spiral.png`, `Logs/phase069-s23-log.txt`, and
  `Logs/phase069-s23-window.txt`. The captured crash scan was clean.
- Not physically validated: final human pose/material acceptance, sustained
  FPS and thermals, deliberate left/right landscape rotation, full route
  traversal, pause/resume, and long-session comfort.

## Phase 0.6.8B Evidence

- Foundation validator: passed in `Logs/phase068b-foundation-validator.log`.
- Source validator: passed in `Logs/phase068b-source-validator.txt`.
- EditMode: `183/183` passed in `Logs/phase068b-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase068b-playmode-results.xml`.
- Rig import audit:
  `Logs/phase068b-hand-rig-handedness-and-material-recovery-audit.txt`
  records the source GLB, mesh/skin/material/texture counts, 23-joint rig,
  source-right/baked-left handedness, positive runtime scales, tangent/material
  recovery, reduced finger/thumb curl, and static wrist settle.
- Production-hierarchy HandPoseLab render: `Logs/HandPoseLab/final.png`.
  Direction proof is in `Logs/HandPoseLab/final-axis-acceptance.txt`; current
  dots are finger-forward `0.9651`, inward-palm `0.1720`, and thumb-up
  `0.1374` on both sides. The low positive palm/thumb values reflect the
  flatter hand-back approval pose. `Logs/HandPoseLab/orientation-report.txt`
  confirms no negative scale and a baked X-mirrored mesh pair.
- Android IL2CPP ARM64: succeeded with zero errors in
  `Logs/phase068b-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.8b-hand-rig-handedness-and-material-recovery-dev.apk`,
  167,317,614 bytes, SHA-256
  `DE8D2AAA77C9CE62F0370D9C61921B3F6E04E23B9EA7B9FCC7F25FF1BEBD29C6`.
- S23 install/launch/Spiral smoke: installed on Samsung Galaxy S23 `SM-S911B`
  (`RFCW100J1HW`), focused `com.unity3d.player.UnityPlayerActivity`, rendered
  fullscreen landscape at 2340x1080, showed the 0.6.8B title/menu after a tap,
  and loaded Spiral with both recovered rigged hands visible from the lower
  corners. The diagnostics overlay reported `FPS 60`, `Ground True`, and
  `PARKOUR_RESPONSIVE_PHYSICAL_V2`. Evidence:
  `Logs/phase068b-s23-menu.png`, `Logs/phase068b-s23-spiral.png`,
  `Logs/phase068b-s23-spiral-log.txt`, and
  `Logs/phase068b-s23-spiral-window.txt`.
- Crash scan: no fatal Android/managed exception patterns were found in the
  captured Spiral launch slice.
- Not physically validated: final human left/right handedness acceptance,
  final material/readability acceptance, sustained FPS, thermals, deliberate
  left/right landscape rotation checks, full route traversal, pause/resume, and
  long-session comfort.

## Phase 0.6.8 Evidence

- Source validator: passed.
- EditMode: `183/183` passed in `Logs/phase068-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase068-playmode-results.xml`.
- Rig import audit: `Logs/phase068-rigged-hand-pose-recovery-audit.txt`
  records the source GLB, mesh/skin/material/texture counts, 23-joint rig,
  baked relaxed finger/thumb curl, static wrist settle, positive-scale mirrored
  prefabs, and `ForearmEntrySleeve` visual cutoff cover.
- Production-hierarchy HandPoseLab render: `Logs/HandPoseLab/final.png`.
  Direction proof is in `Logs/HandPoseLab/final-axis-acceptance.txt`; current
  dots are finger-forward `0.9651`, inward-palm `0.5668`, and thumb-up
  `0.5337` on both sides. `Logs/HandPoseLab/orientation-report.txt` confirms
  no negative scale and a baked X-mirrored mesh pair.
- Android IL2CPP ARM64: succeeded with zero errors and one warning in
  `Logs/phase068-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.8-rigged-hand-pose-recovery-dev.apk`,
  167,455,364 bytes, SHA-256
  `A5185EDB2F743A3AE3907074E033D2D0210A1211E2D0ABFDF59D2A5CCCF8F9C7`.
- S23 install/launch/Spiral smoke: installed on Samsung Galaxy S23 `SM-S911B`
  (`RFCW100J1HW`), focused `com.unity3d.player.UnityPlayerActivity`, rendered
  fullscreen landscape at 2340x1080, showed the 0.6.8 title/menu after a tap,
  and loaded Spiral with both recovered rigged hands visible from the lower
  corners. The diagnostics overlay reported `FPS 60`, `Ground True`, and
  `PARKOUR_RESPONSIVE_PHYSICAL_V2`. Evidence:
  `Logs/phase068-s23-launch-after-tap.png`,
  `Logs/phase068-s23-spiral.png`, `Logs/phase068-s23-spiral-log.txt`, and
  `Logs/phase068-s23-spiral-window.txt`.
- Crash scan: no fatal Android/managed exception patterns were found in the
  captured Spiral launch slice.
- Not physically validated: human rigged-pose acceptance, sustained FPS,
  thermals, deliberate left/right landscape rotation checks, full route
  traversal, jump/fall/readability feel, and pause/resume.

## Phase 0.6.7 Evidence

- Source validator: passed.
- EditMode: `183/183` passed in `Logs/phase067-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase067-playmode-results.xml`.
- Rig import audit: `Logs/phase067-rigged-arm-audit.txt` records the source
  GLB, copied project asset, mesh/skin/material/texture counts, baked left
  mirror, positive runtime scales, preserved 0.6.6 camera-space anchors, and
  finger chains.
- Production-hierarchy HandPoseLab render: `Logs/HandPoseLab/final.png`.
  Direction proof is in `Logs/HandPoseLab/final-axis-acceptance.txt`.
- Android IL2CPP ARM64: succeeded with zero errors and one warning in
  `Logs/phase067-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.7-safe-rig-integration-and-locomotion-dev.apk`,
  167,453,368 bytes, SHA-256
  `8AC7BB796A49B648C8AEF9E33163C4CEF08D0F9D3E268A58F53E3B6ED47BF922`.
- S23 install/launch smoke: installed on Samsung Galaxy S23 `SM-S911B`
  (`RFCW100J1HW`), focused `com.unity3d.player.UnityPlayerActivity`, rendered
  fullscreen landscape at 2340x1080, and the captured gameplay frame shows the
  0.6.7 diagnostics overlay with both rigged hands visible. Evidence:
  `Logs/phase067-s23-launch-2.png`,
  `Logs/phase067-s23-launch-log-2.txt`, and
  `Logs/phase067-s23-window.txt`.
- Crash scan: no fatal Android/managed exception patterns were found in the
  captured launch slice.
- Not physically validated: human rigged-pose acceptance, deformation during
  run/jump/fall/landing animation, full route traversal, sustained FPS,
  thermals, and pause/resume.

## Phase 0.6.6 Evidence

- Source validator: passed.
- Hand-focused EditMode: `6/6` passed across
  `Logs/phase066-relevant-editmode-results.xml`,
  `Logs/phase066-pose-editmode-results.xml`, and
  `Logs/phase066-handposelab-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase066-playmode-results.xml`.
- Production-hierarchy HandPoseLab render: `Logs/HandPoseLab/final.png` at
  2340x1080. Direction proof is in
  `Logs/HandPoseLab/final-axis-acceptance.txt`.
- Android IL2CPP ARM64: succeeded with zero errors and one legacy-icon warning
  in `Logs/phase066-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.6-first-person-hand-pose-lock-dev.apk`,
  164,492,684 bytes, SHA-256
  `412DA9821A86B9BCD3685B10E085DF86D784747DB0CDDF88793B419A93010DD9`.
- Install/launch smoke was not performed because ADB reported no connected
  device on two checks. Human physical pose acceptance remains pending.

## Phase 0.6.5 Evidence

- Source validator: passed.
- EditMode: `182/182` passed in `Logs/phase065-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase065-playmode-results.xml`.
- Android IL2CPP ARM64: succeeded with zero errors and one legacy-icon warning
  in `Logs/phase065-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.5-surgical-recovery-and-correct-hand-pose-dev.apk`,
  164,481,736 bytes, SHA-256
  `6A50AE3017EE8DCB7817BBD2621A1B4C1AEB0316EE392D8A3293BC901F44CF3D`.
- S23 install/launch smoke: installed on Samsung Galaxy S23 `SM-S911B`
  (`RFCW100J1HW`), focused `com.unity3d.player.UnityPlayerActivity`, rendered
  fullscreen at 2340x1080 landscape, and the second capture shows the 0.6.5
  diagnostics overlay with both hands visible. Evidence:
  `Logs/phase065-s23-title-2.png`,
  `Logs/phase065-s23-launch-log-2.txt`, and
  `Logs/phase065-s23-window-2.txt`.
- Crash scan: no fatal Android/managed exception patterns were found in the
  captured launch slice.
- Not physically validated: final human hand-pose acceptance, dark rectangle
  absence from all gameplay viewpoints, full route traversal, sustained FPS,
  thermals, and pause/resume.

## Phase 0.6.4 Evidence

- Source validator: passed.
- EditMode: `182/182` passed in `Logs/phase064-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase064-playmode-results.xml`.
- Android IL2CPP ARM64: succeeded with zero errors and one legacy-icon warning
  in `Logs/phase064-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.4-natural-hand-pose-and-world-composition-pass-dev.apk`,
  164,504,168 bytes, SHA-256
  `B5EB0C943C811707428ED52B19B7645D455E3A70C6AE09960558A505E44A48A2`.
- S23 install/launch smoke: a later explicit install/run request succeeded on
  Samsung Galaxy S23 `SM-S911B` (`RFCW100J1HW`) at 2340x1080 fullscreen
  landscape with no fatal Android/managed exception patterns in the captured
  launch slice. This was launch smoke only.
- Not physically validated: final hand-pose acceptance, water/flow readability,
  world composition cohesion, exposed-map false-platform scan, Spiral abyss
  mood from gameplay, full Spiral traversal, sustained FPS, thermals, and
  pause/resume.

## Phase 0.6.3 Evidence

- Source validator: passed.
- EditMode: `180/180` passed in `Logs/phase063-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase063-playmode-results.xml`.
- Android IL2CPP ARM64: succeeded with zero errors and one legacy-icon warning
  in `Logs/phase063-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.3-hand-pose-collision-truth-abyss-depth-dev.apk`,
  164,488,244 bytes, SHA-256
  `8604F583934CAB7A61984D8344D28947CD9043E0A484660AB863684DA0BA2C23`.
- S23 install/launch smoke: installed on Samsung Galaxy S23 `SM-S911B`
  (`RFCW100J1HW`), focused `com.unity3d.player.UnityPlayerActivity`, and
  rendered fullscreen at 2340x1080 landscape. The second title capture shows
  the 0.6.3 diagnostics overlay at 60 FPS. Evidence:
  `Logs/phase063-s23-title-2.png`,
  `Logs/phase063-s23-launch-log-2.txt`, and
  `Logs/phase063-s23-window-2.txt`.
- Crash scan: no fatal Android/managed exception patterns were found in the
  captured launch slice.
- Not physically validated: final human hand-pose acceptance, Module 03 water
  readability, other exposed-map false-platform scan, Spiral abyss mood from
  gameplay, full Spiral traversal, sustained FPS, thermals, and pause/resume.

## Phase 0.6.2 Evidence

- Source validator: passed.
- EditMode: `178/178` passed in `Logs/phase062-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase062-playmode-results.xml`.
- Android IL2CPP ARM64: succeeded with zero errors and one legacy-icon warning
  in `Logs/phase062-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.2-s23-feedback-polish-dev.apk`,
  164,474,952 bytes, SHA-256
  `60F57373EE3B1F0A968F7EC71798F3291CE1357D2BAD35489EE79C52BA459C50`.
- S23 install/launch smoke: installed on Samsung Galaxy S23 `SM-S911B`,
  focused `com.unity3d.player.UnityPlayerActivity`, and rendered fullscreen at
  2340x1080. A live campaign level loaded at 60 FPS with the revised hands
  visible. Evidence: `Logs/phase062-s23-flow-error-entry.png`,
  `Logs/phase062-s23-launch-log.txt`, and `Logs/phase062-s23-window.txt`.
- Not physically validated: final human hand-pose acceptance, full Spiral
  route, lower-void mood from Spiral gameplay, sustained FPS, thermals, and
  pause/resume.

## Phase 0.6.1 Evidence

- Source validator: passed.
- EditMode: `178/178` passed in `Logs/phase061-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase061-playmode-results.xml`. This
  includes the title/menu artifact regression and Spiral crumble contact/reset
  coverage.
- Android IL2CPP ARM64: succeeded with zero errors and one legacy-icon warning
  in `Logs/phase061-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.1-parkour-feel-and-route-rhythm-dev.apk`,
  164,474,820 bytes, SHA-256
  `7E2C2B120BEC6117A95E0767C7A9B3F27E9C7ADE9F27E2B38922B7A52CE37617`.
- S23 install/launch smoke: title UI rendered at 2340x1080 fullscreen over the
  approved panorama with the traced rectangle gone; no fatal exception was
  logged in the captured launch slice. Evidence:
  `Logs/phase061-s23-title-2.png`,
  `Logs/phase061-s23-launch-log-2.txt`, and
  `Logs/phase061-s23-window-2.txt`.
- Not physically validated: human one-second crumble feel, full Spiral route,
  hand motion while playing, sustained FPS, thermals, pause/resume, and final
  visual acceptance.

## Phase 0.6.0 Evidence

- Source validator: passed.
- EditMode: `176/176` passed in `Logs/phase060-editmode-results.xml`.
- PlayMode: `6/6` passed in `Logs/phase060-playmode-results.xml`. This
  includes real CharacterController floor contact, collapse, and reset against
  a runtime-instantiated Spiral crumble block.
- Android IL2CPP ARM64: succeeded with zero errors and one legacy-icon warning
  in `Logs/phase060-android-build.log`.
- APK:
  `Builds/Android/RYDERS-ROAD-0.6.0-crumble-route-truth-and-hand-refinement-dev.apk`,
  164,472,892 bytes, SHA-256
  `8036FB1B4FFAC83992400EBEF49FC9FAF3545873419958AEA3735F88588AA5CF`.
- S23 install/launch smoke: title and grounded Spiral start rendered at
  2340x1080 fullscreen and 60 FPS at rest; no fatal exception was logged.
  Evidence: `Logs/phase060-s23-title.png`,
  `Logs/phase060-s23-spiral-start.png`, and
  `Logs/phase060-s23-spiral-log.txt`.
- Not physically validated: landing-triggered crumble lifecycle, reset after
  death/Restore, required moving crossing, full Bronze route, hands in motion,
  sustained performance, thermals, and pause/resume.

## Phase 0.5.9 Evidence

- Source validator: passed.
- EditMode: `175/175` passed in `Logs/phase059-editmode-results.xml`.
- PlayMode: `5/5` passed in `Logs/phase059-playmode-results.xml`.
- Android IL2CPP ARM64 build: zero errors and one legacy-icon warning in
  `Logs/phase059-android-build.log`.
- S23 `SM-S911B`: install and launch passed; title and grounded Spiral start
  rendered at 2340x1080, fullscreen, fixed landscape, 60 FPS at rest, no crash.
- Device evidence: `Logs/phase059-s23-launch-2.png`,
  `Logs/phase059-s23-spiral-launch.png`,
  `Logs/phase059-s23-final-launch.png`, and
  `Logs/phase059-s23-launch-log.txt`.
- Not physically validated: crumble interaction/reset, full Bronze route,
  Restore feel, moving bridge traversal, touch comfort, sustained performance,
  thermals, and pause/resume.

Focused crumble tests cover all three phases, repeated activation immunity,
visual-only shake, collider disable, and deterministic reset.

## Pinned Environment

- Unity Editor target: `6000.5.6f1`
- Packages: Input System `1.20.0`, URP `17.5.0`, Test Framework `1.7.0`
- Android: IL2CPP, ARM64, minimum API 26
- Runtime: fixed landscape-left, 60 FPS target
- Current build: `0.6.4-natural-hand-pose-and-world-composition-pass`
- Automated test count: 188 passed (182 EditMode, 6 PlayMode, 0 failed)

## Phase 0.5.8 Verification Record

- Source validation and Unity project validation passed on 2026-08-21.
- EditMode: `174/174` passed; `Logs/phase058-editmode-results-pass2.xml`.
- PlayMode: `5/5` passed; `Logs/phase058-playmode-results.xml`.
- Android IL2CPP ARM64 clean development build succeeded with zero errors and
  one nonfatal legacy-icon deprecation warning; `Logs/phase058-android-build.log`.
- APK: `Builds/Android/RYDERS-ROAD-0.5.8-s23-physical-acceptance-and-targeted-world-polish-dev.apk`,
  `160,687,330` bytes, SHA-256
  `97B08A6917E9C55D183D4148053AE44A8847F0256C965650B21DD257777E4D0E`.
- Manifest `screenOrientation=landscape`; S23 runtime bounds were 2340x1080
  fullscreen at rotation 90, replacing the prior portrait-letterbox failure.
- S23 install, title load, and grounded Spiral-start load passed at 60 FPS.
  Evidence: `Logs/device-058-title.png` and
  `Logs/device-058-spiral-start.png`.
- This was a bounded launch/load smoke, not manual gameplay. Revised hands,
  boost landing, lower crumble line, complete Spiral, sustained FPS, thermals,
  pause/resume, and fixed-orientation comfort still need human review.

## Phase 0.5.7 Verification Record

- Source and Unity project validation passed on 2026-08-21.
- EditMode: `172/172` passed; `Logs/phase057-editmode-results.xml`.
- PlayMode: `5/5` passed; `Logs/phase057-playmode-results.xml`.
- Android IL2CPP ARM64 development build succeeded with zero errors and one
  legacy-icon deprecation warning; adaptive icons are configured;
  `Logs/phase057-android-build.log`.
- APK: `Builds/Android/RYDERS-ROAD-0.5.7-world-cohesion-and-gameplay-presentation-dev.apk`.
- Samsung S23 `SM-S911B` install and launch passed. The repaired final APK
  reached the title and Spiral start at 60 FPS with `Ground True`, no
  SphereCollider/cloud-instantiation errors, no development console, and no
  app crash. Evidence: `Logs/device-057-title-final.png` and
  `Logs/device-057-spiral-start-fixed.png`.
- Human thumb gameplay, collision edges, death Restore heading feel, complete
  route traversal, sustained FPS, and thermals remain pending.
- New coverage locks canonical Standard/Safe/Long visual selection, authored
  colliders, death Restore yaw, authored initial yaw, explicit Spiral world
  layers, crumble-role identity, and limited modular fallback.

## Phase 0.5.6 Verification Record

- `Tools/Validate-Foundation.ps1`: passed on 2026-08-21.
- EditMode: `167/167` passed; result file `Logs/editmode-056-final.xml`.
- PlayMode: `5/5` passed; result file `Logs/playmode-056-final.xml`.
- Android: IL2CPP ARM64 development APK built with zero reported warnings/errors; log `Logs/android-build-056.log`.
- Device smoke: installed and launched on Samsung Galaxy S23 `SM-S911B` (`RFCW100J1HW`). Spiral entered on a solid start, diagnostics held 60 FPS at rest, fullscreen rendered without system bars, and forced landscape rotations 1 and 3 both rendered at 2340x1080. Auto-rotate was restored afterward.
- Device visuals: production hands visibly point forward/down from the lower corners and the procedural sky showed no vertical seam.
- Not physically approved: ADB synthetic swipes did not reliably drive Unity gameplay input, so deliberate fall/Restore, first-five-jump feel, complete Bronze traversal, and in-route Moving/Boost/Crumble/Surf/Patch interaction still require human thumb play. The PlayMode fall test proves one prompt Restore after crossing the threshold but is not a physical-device claim.

Phase 0.5.6 adds focused coverage for explicit start support, ten deterministic
Spiral start resolutions, module fall recovery, measured movement bounds,
Spiral rhythm/spacing, role visual tiling, procedural sky configuration,
HandPoseLab references, and current benchmark anchors.

Unity compilation, Unity Test Runner execution, and APK generation require
Unity with Android Build Support plus JDK, Android SDK, and Android NDK.
Source-only validation is useful preflight evidence but is not a substitute
for Unity tests or physical-device testing.

This PC uses local Android tool overrides:

```powershell
$deps = Join-Path $env:LOCALAPPDATA 'RydersBlock\AndroidDeps'
$env:RYDERS_BLOCK_ANDROID_JDK = Join-Path $deps 'OpenJDK17'
$env:RYDERS_BLOCK_ANDROID_NDK = Join-Path $deps 'NDK-r27c'
$env:RYDERS_BLOCK_ANDROID_SDK = Join-Path $deps 'AndroidSDK'
```

## First Open

1. Install Unity `6000.5.6f1` with Android Build Support, SDK/NDK, and OpenJDK.
2. Open `C:\Users\lin4s\Documents\Riders Block` and wait for import and
   compilation.
3. Run `RYDERS BLOCK > Apply Project Settings`.
4. Run `RYDERS BLOCK > Validate Project`.
5. Open `Assets/_Game/Levels/Scenes/Bootstrap.unity` and enter Play Mode.
6. Confirm Bootstrap loads `ModuleSelector`.

## Editor Movement Check

Use the Game view at 1920 x 1080, 2400 x 1080, 2340 x 1080, and 1280 x 720.

- WASD moves; mouse looks; Space jumps; R restores.
- Escape releases the Editor pointer and left click captures it again.
- Movement, look, and jump work simultaneously.
- F1 toggles Normal and Full Diagnostics.
- F2 cycles Default, Forgiving, Precise, and Experimental profiles.
- F3 toggles touch-zone visualization.
- F4 toggles camera bob, landing response, smoothing, and speed FOV.
- `ModuleSelector` can launch MovementLab and Modules 01, 02, 03, and 04.
- `ModuleRunner` shows the selected module name, timer, rank target, restores,
  and falls.
- Patch Block completion opens results with rank, time, PB state, score, next
  rank gap, Retry, Next Module, and Module Select.
- The top-left run menu opens and closes without moving the player, and its
  Retry, Module Select, Look sensitivity, and touch-zone controls work.
- The development `DEV` drawer exposes `MENU`, `RESTART`, `RESTORE`, `LABELS`,
  `DIAG`, and other debug controls without crowding normal gameplay.
- The development `DEVTP` control and `F6` benchmark teleport cycle anchors
  and mark the run invalid for personal-best saving.
- Editor controls remain usable in MovementLab and ModuleRunner.
- Touch module play starts in Classic Mode: left-thumb movement, right-thumb
  camera drag, and right-side Tap Jump.
- The run menu `MODE` button switches between Classic and Easy. Easy uses
  left-thumb movement/steering, broad right-side Jump Zone, and Smart Parkour
  Camera.
- `CTRL`, `JUMP`, `LOOK`, and `MOVE` development buttons cycle control
  profile, jump mode, camera sensitivity, and movement sensitivity at runtime.
- F3 touch-zone visualization shows broad look/move regions and the movement
  origin/travel area; the left movement ring is otherwise visible only while
  active.
- MovementLab contains a Jump Calibration Lane with standing, base-speed,
  diagonal, running, bhop, high-momentum, air-strafe, lower-platform, and
  higher-platform targets.

Editor checks do not validate real multitouch, comfort, safe areas on hardware,
phone performance, thermals, suspend/resume, or device-specific behavior.

## Phase 2 Module Checks

Module 01, `module.001.first-steps`:

- Route starts safely and teaches normal blocks, small gaps, boost, Restore
  Point, and Patch Block completion.
- Missing a jump falls into Null Space and restores to the latest Restore
  Point.

Module 02, `module.002.moving-parts`:

- Moving blocks carry the player consistently.
- Crumbling blocks warn and disappear, then reset after restore/restart.
- Shortcuts record local telemetry without unlocking progression.

Module 03, `module.003.flow-error`:

- The route reads as a compact vertical climb around a central structure.
- Water flow, boosts, moving blocks, and Restore Points preserve upward
  progress.
- The Patch Block can be reached without changing movement tuning.

Module 04, `module.004.the-spiral`:

- The route reads as a large vertical spiral around a broken floating tower.
- Five Restore Points divide the map into fair sections.
- Standard completion does not require bhop, air-strafe, surf, shortcut
  routing, or boost overshoot.
- The broad Bronze spine keeps ordinary adjacent platform gaps phone-friendly;
  `Phase049SpiralShowcase_BronzeSpineCapsPhoneHostileGaps` guards the current
  spacing.
- Water, boost, moving, crumbling, and optional surf mechanics are visible
  without relying on debug labels.
- `DEVTP`/`F6` can jump to start, low, mid, surf, high, and summit review
  anchors and invalidates PB eligibility.

## Phase 0.5.0 Meshy Art-Kit Checks

- `MeshyArtKitIntegrator.IntegrateMeshyArtKit` completes without compile or
  asset import errors.
- `ART_ASSET_MANIFEST.md` lists all imported environment roles and records the
  optimized `RR_FP_Arm_Right` source at exactly 15,400 triangles.
- `ModuleVisualPrefabLibrary.asset` maps platform, Restore, Boost, Patch,
  tower, nature, arch, and cyan-energy roles to production prefabs.
- Meshy runtime prefabs have renderers, no child colliders, no invalid shaders,
  and motion vectors disabled.
- Standard and long normal blocks resolve to different prefab placements based
  on authored block size.
- `ArtKitGallery.unity` opens in the editor for review but remains absent from
  Android Build Settings.
- `FirstPersonArmProfile.asset` resolves both optimized runtime arm prefabs.
- Runtime arm prefabs have no colliders and all runtime transform scales are
  positive; the generated left-arm mesh preserves UVs/materials while mirroring
  normals/tangents and triangle winding.

## Phase 3 Ranking Checks

- Any valid completion earns at least Bronze.
- Falling and restoring do not reset the run timer.
- `RESTART` resets timer/run state by reloading the module.
- Explicit timer stop/pause prevents elapsed time from advancing until the
  timer starts again; mobile app backgrounding does not advance gameplay time
  while Unity updates are suspended.
- Restore Points show split feedback and PB deltas where available.
- Results show Bronze/Silver/Gold/Diamond, score, PB state, and next-rank gap.
- Retry returns quickly to the same module.
- Campaign Module 01 starts unlocked, Module 02 unlocks after Module 01
  completion, and Module 03 unlocks after Module 02 completion. THE SPIRAL is
  a separate direct mode, not a fourth Campaign unlock.
- Higher ranks are never required for normal progression.
- Locked modules can be launched only as development override runs in Editor or
  development builds; those runs must not overwrite PBs.

## Phase 3B Visual Checks

- `Docs/VisualReferences/target_gameplay.png` remains the primary visual
  reference.
- The sky shows blue/violet depth, cyan midtone, warm horizon, and simple
  cloud layers.
- Null Space reads as deep blue/purple atmospheric depth, not a flat plane.
- Gameplay platforms have light readable tops, darker sides, and darker
  undersides.
- Floating islands, towers, clouds, and vegetation are decorative and do not
  hide jump edges or look like required route platforms.
- Water is cyan/transparent with directional streaks that match flow.
- Restore, Patch, Boost, Moving, and Crumbling roles are recognizable without
  text.
- First-person gloves sit in the lower corners and do not block landing zones.
- HUD and controls remain readable in landscape and avoid the central route.
- `F6` cycles development benchmark views in `ModuleRunner`.

## Phase 0.4.0 Visual Identity Checks

- `ModuleEnvironmentProfile` uses `MobileBalanced` as the phone quality target.
- `ModuleVisualProfile` includes surface highlight, inset, circuit line, warm
  accent, flower accent, HUD opacity, trim scale, and hero density fields.
- Route blocks keep exact gameplay colliders while visual-only child pieces add
  tops, insets, trims, side shade, undersides, and small circuit ticks.
- Normal, precision, and crumbling route blocks can add visual-only flow
  chevrons from authored block order; moving, boost, water, and surf blocks use
  mechanic-specific direction cues instead.
- Module 03 includes the authored hero dressing islands, towers, trees, and
  cloud frames without adding new playable surfaces.
- Water, boost, patch, restore, moving, and crumbling cues are identifiable
  without relying on labels.
- `F6` benchmark anchors include `Benchmark_Module01_Start`,
  `Benchmark_Module02_Mid`, `Benchmark_Module03_Hero`, and the optional
  `Benchmark_Module03_LookDown` / `Benchmark_Module03_LookUp` views.
- First-person gloves remain below the landing zone after jump, landing,
  boost, and water visual reactions.
- First-person glove pieces are visual-only: no colliders, shadows, reflection
  probes, light probes, dynamic occlusion, or motion vectors.

## Phase 0.4.6 Alpha Vertical Slice Checks

- `THE SPIRAL` has stable ID `module.004.the-spiral`.
- The route has six major sections and five Restore Points plus start.
- The summit Patch Block completes the module once.
- Optional shortcuts are metadata/triggers only and do not block Bronze
  completion.
- Authored surf surfaces are optional momentum lines, not required route
  surfaces.
- Module diagnostics include available renderer, shadow-caster, realtime
  light, collider, active rigidbody, particle-system, boost, water, surf, and
  peak-speed counters.
- Provisional rank thresholds remain uncalibrated until physical Android
  timing data exists.
- Pooled bursts and visual pulses do not create gameplay dependencies or
  excessive persistent emitters.
- Visual changes do not modify `ParkourMotor`, input contracts, camera input
  ownership, Restore, rank, score, progression, or save behavior.

## Parkour Movement Control Checks

- Left thumb can move forward/back/strafe from the inner-left screen region.
- Right thumb can look/yaw/pitch from the inner-right screen region.
- Touches that begin near a bezel clamp the effective movement origin inward
  so the full travel circle remains inside safe area plus comfort margins.
- A normal upward movement drag runs forward without jumping.
- Holding left-thumb forward for 10 seconds produces no repeated jumps.
- A short right-region tap emits one JumpIntent on release while preserving
  movement ownership, movement vector, joystick origin, jump buffer, and coyote
  time.
- Right-region drag crosses the drag threshold quickly, controls camera, and
  never jumps on release.
- Left movement touches never jump.
- Repeated right taps while left movement is held keep movement active.
- Flick Jump is not part of the default player-facing profile.
- Momentum, bhop retention, air-strafe correction, boost exits, water flow,
  and authored surf surfaces do not invalidate unassisted runs.

## Movement V1 Control Checks

- Classic runtime profile is `LeftMoveRightLookTapJump` with `RightTap`.
- Left thumb moves camera-relative forward/back/strafe.
- Right-thumb drag rotates yaw/pitch with no roll.
- Right-thumb drag starts feeding camera input on the drag-activation frame.
- A short right-side tap emits one JumpIntent on release while left movement
  remains held.
- Drag activation starts camera promptly and never emits Jump on release.
- Tiny right-side tap jitter still counts as Tap Jump and does not visibly jerk
  the camera.
- Flick Jump, fixed Jump, right jump zones, and flow steering are not the
  default player-facing profile.
- Updated installs that saved an old Easy, legacy, or development alpha
  control profile migrate once back to Classic; choosing another mode again
  after that remains persistent.
- Manual right-thumb look overrides landing-awareness/auto-pitch helpers.
- Dynamic FOV stays within the configured cap during run, air, surf, and
  momentum movement.
- Android diagnostics in Full mode show resolution, safe area, orientation,
  fullscreen state, immersive request state, focus state, and Android API.

## Smart Parkour Camera Checks

- Easy Mode uses `FlowSteerAutoBalanced`, `RightJumpZone`, and
  `AutoCameraProfileKind.SmartParkour`.
- Left thumb movement/steering and right-side jump remain the only player
  inputs needed for Easy Mode.
- Smart Camera yaw is rendered as a camera offset and does not continuously
  rotate the player body or modify `ParkourMotor` velocity.
- Camera cone inner/outer response has a quiet dead zone and smooth turn-in.
- Filtered velocity drives look-ahead at speed; raw stick noise does not jerk
  the camera when velocity is clear.
- Stationary/low-speed steering can gently frame the intended start direction.
- Route graph guidance stays weak, broad, and off when the player is far from
  a corridor.
- Forks stay neutral with weak/equal evidence, lean with growing evidence, and
  commit only after sustained intent.
- Branch hysteresis rejects brief steering noise and avoids flickering across
  parallel paths.
- Shortcuts can be framed when velocity/steering/position agree, then the
  standard route reacquires after a merge.
- U-turns, restores, boosts, water, surf, airborne falls, and stopping do not
  trigger auto-jump, route selection, landing magnetism, or fall prevention.
- Classic Mode manual right-thumb camera remains unchanged.

## Phase 0.4.7 Brand Alpha Checks

- Android product label is `Ryder's Road`.
- Player-facing title/logo is `RYDER'S ROAD`.
- Package ID remains `com.rydersblockstudio.rydersblock`.
- `Assets/Branding/Source` contains untouched supplied logo/icon sources.
- Runtime logo loads from `Resources/Branding/RydersRoad_Logo_UI`.
- Android legacy and adaptive icon textures are assigned by project setup.
- Icon mask previews exist under `Docs/Branding`.
- `ModuleSelector` opens with the supplied logo, `CAMPAIGN`, `SPIRAL`, and
  `SETTINGS`.
- `CAMPAIGN` opens the three-level Campaign selector without listing Spiral.
- `SPIRAL` enters `THE SPIRAL` directly.
- `SETTINGS` exposes Easy/Classic control, steering speed, look sensitivity,
  FOV, graphics quality, haptics, and audio controls.
- Run menu keeps Easy/Classic and look controls while using the new title.
- Product-name save-path migration copies old editor/desktop save files only
  when the current path has no save.
- Android fullscreen settings include no requested visible insets, Render
  Outside Safe Area, FullScreenWindow, and transient system bars.
- Runtime fullscreen applies modern insets and legacy immersive flags, then
  reapplies during startup, resume, and focus return.
- Samsung Galaxy S23 still needs physical confirmation that the top status bar
  stays hidden and that the responsive Classic default feels better than the
  previous Easy launch profile.

## Unity Test Runner

Open `Window > General > Test Runner` and run all Edit Mode and Play Mode tests.
The Edit Mode suite covers service composition, movement math/state,
camera-relative input vectors, joystick center/dead-zone math, Restore Point
state, touch ownership, profile selection, moving-platform math, module
definition validation, module mechanics math, module assets, run timer, rank
boundaries, monotonic score, personal bests, progression unlocks, split
comparison, motor reset, camera reset, URP gameplay material validation, saves,
flags, and utility contracts. The Play Mode suite covers Bootstrap-to-ModuleSelector loading,
title/menu artifact regressions, MovementLab availability, selected ModuleRunner
loading for all four current modules, Crumble contact/reset behavior, Spiral
start/Restore safety, and Null Space Restore behavior.

Batch equivalent:

```powershell
$unity = 'C:\Program Files\Unity\Hub\Editor\6000.5.6f1\Editor\Unity.exe'
& $unity -batchmode -nographics -projectPath 'C:\Users\lin4s\Documents\Riders Block' -runTests -testPlatform EditMode -testResults 'C:\Users\lin4s\Documents\Riders Block\Logs\editmode-results.xml' -logFile 'C:\Users\lin4s\Documents\Riders Block\Logs\editmode.log'
& $unity -batchmode -nographics -projectPath 'C:\Users\lin4s\Documents\Riders Block' -runTests -testPlatform PlayMode -testResults 'C:\Users\lin4s\Documents\Riders Block\Logs\playmode-results.xml' -logFile 'C:\Users\lin4s\Documents\Riders Block\Logs\playmode.log'
```

Unity Test Framework `1.7.0` does not run command-line tests when `-quit` is
passed with `-runTests`.

## Source-Only Validation

```powershell
& 'C:\Users\lin4s\Documents\Riders Block\Tools\Validate-Foundation.ps1'
```

This checks repository structure, JSON, assembly references, build-scene order,
configuration identity, Phase 3 module/rank assets, generated scenes, visual
profiles, 0.4.6B Smart Parkour Camera files, 0.4.7 brand assets, readable
uncompressed Android launcher icons, Easy/Classic touch configuration, Android
fullscreen settings, benchmark docs, V4 save migration files, and required
movement profiles.

Save EditMode coverage includes V1-to-current migration, V3-to-V4
economy/inventory migration, stable currency add/spend, stackable item grants,
cosmetic equip gating by unlock state, valid-only module completion rewards,
and duplicate protection through claimed reward IDs.

## Android Development APK

Use `RYDERS BLOCK > Build > Android Development APK`, or:

```powershell
$deps = Join-Path $env:LOCALAPPDATA 'RydersBlock\AndroidDeps'
$env:RYDERS_BLOCK_ANDROID_JDK = Join-Path $deps 'OpenJDK17'
$env:RYDERS_BLOCK_ANDROID_NDK = Join-Path $deps 'NDK-r27c'
$env:RYDERS_BLOCK_ANDROID_SDK = Join-Path $deps 'AndroidSDK'
$unity = 'C:\Program Files\Unity\Hub\Editor\6000.5.6f1\Editor\Unity.exe'
& $unity -batchmode -nographics -quit -projectPath 'C:\Users\lin4s\Documents\Riders Block' -buildTarget Android -executeMethod Avoidance.EditorTools.AndroidDevelopmentBuilder.BuildAndroidDevelopment -logFile 'C:\Users\lin4s\Documents\Riders Block\Logs\android-build.log'
```

Expected output:
`Builds/Android/RYDERS-ROAD-0.8.1-module003-hand-authored-ancient-abyss-dev.apk`.

Install that exact build on a real Android phone and complete the Phase 2,
Phase 3/3B/Movement V1, and THE SPIRAL sections in `BETA_FEEDBACK.md`. Do
not approve touch controls, pacing, rank thresholds, visual readability, safe
areas, or performance from PC-only evidence.

## Phase 0.8.0 Module 003 Checks

The full Module 003 PlayMode audit traverses all 27 ordinary Bronze supports
and checks visual/collider truth, three Crumble blocks, hidden arms, disabled
grounding cue, bounded VFX pooling, absence of radial mist/world floors, and
absence of obsolete giant-tower and mist-card objects. The global smoke suite
continues loading Modules 001/002 and Spiral without authoring changes.

`Module003_CapturesFiveProductionGameplayViews` renders opening, early, middle,
high/exposed, and Patch-approach PNGs to `Logs/Phase080VisualQA`. Passing the
test only proves renderability and luminance range; a human must still inspect
all five images for the visible major-version quality gate.

The content-version-4 correction adds deterministic coverage for the shipped
Boost arc and Moving ferry. Boost coverage uses the default movement profile's
asymmetric ascent/descent gravity and checks nominal center arrival plus slow
and full-approach landing margins. Moving coverage checks useful travel
distance, sub-three-second one-way timing, setup/exit boarding gaps, and that
the direct route gap genuinely requires the ferry.

## Phase 0.8.1 Module 003 Checks

Module 003 content version 5 replaces the old path guide and dust fields with
five collider-free authored place prefabs. EditMode verifies the nine-object
Ancient Abyss envelope, five near-place identities, near/mid/far/below depth
bands, no legacy causeway or reclaimed-sanctuary entries, no sandstone slab
materials in the playable place set, three optional shortcuts, and the exact
Moving and Boost route contracts.

PlayMode verifies every ordinary support, three Crumble blocks, three Restores,
hidden arms, manual camera behavior, bounded feedback, five place foundations,
and the absence of the world floor, mist discs, dust fields, and obsolete
moving bridge guide. `Module003_CapturesFiveProductionGameplayViews` writes
the five accepted views to `Logs/Phase081VisualQA`.

The Ancient Abyss sky regression checks the versioned source is imported as a
seamless cubemap and bound through `Skybox/Cubemap`. This exists because visual
pass three exposed a vertical panorama seam in the Broken Crossing camera.
Automated image creation proves rendering, not art quality; inspect all five
views before accepting the phase.
